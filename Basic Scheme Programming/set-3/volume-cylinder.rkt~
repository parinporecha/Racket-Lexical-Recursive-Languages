#lang racket

;;; volume-cylinder :: number, number -> number
(provide/contract 
 [volume-cylinder (-> number? number? number?)])

(define volume-cylinder 
   (lambda(radius height)
      (* radius radius height 3.1415926535)))